# Creare Funzioni

1. Andare su `src/functions-declarations.ts`
2. Ogni elemento dell'array `FUNCTION_DECLARATIONS` è una route dell'API `https://{API_URL}/{routeName}`.
3. Per aggiungere delle sottoroute, inserire una lista di `subRoutes` con la stessa sintassi.
4. Specificare la function path a partire dalla cartella `src`.